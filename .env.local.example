NEXT_PUBLIC_SUPABASE_URL=https://YOURPROJECT.supabase.co
# server only — the ONLY database credential. RLS is on with no policies,
# so nothing reaches the tables except these routes, behind the PIN.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Who can get in. Name:PIN, comma separated, four digits each. The PIN is the
# identity — it says who you are, and every write is logged under that name.
# Unset = no lock locally, and a deployment that refuses to serve.
ROLODECK_USERS=Name:0000,Other:1111
# Google, server-side only. Powers the invite field's venue autocomplete, the
# From field's place lookup, and the static map. Needs Places API (New) and
# Maps Static API enabled. Never NEXT_PUBLIC — Google bills per call.
GOOGLE_PLACES_API_KEY=your-google-places-key
# Optional, and PUBLIC by nature: set this and the map becomes a real draggable
# Google map instead of a picture. The library runs in the browser, so the key
# ships in the page — make it a SEPARATE key, restricted by HTTP referrer to
# your own domain, with only Maps JavaScript API enabled. The referrer
# restriction is what protects it; secrecy isn't available here.
# Leave it unset to keep the server-drawn static map.
NEXT_PUBLIC_MAPS_KEY=
# Optional. Instagram shows a datacenter IP a login wall, so deployed photo
# pulls come back empty without this. It's the `sessionid` cookie from a browser
# you're logged into — use a throwaway account, and expect to redo it whenever
# that account signs out.
IG_SESSIONID=
